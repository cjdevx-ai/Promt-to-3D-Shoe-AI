version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8001:8080"
    env_file:
      - ./backend/.env
    volumes:
      - ./backend/static/outputs:/app/static/outputs
    restart: always
